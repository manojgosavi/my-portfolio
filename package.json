{
  "name": "my-portfolio",
  "private": true,
  "version": "1.0.0",
  "description": "Source for manojgosavi.github.io — plain static HTML in site/, no build step.",
  "type": "module",
  "scripts": {
    "preview": "python3 -m http.server 8000 -d site",
    "og": "node scripts/generate-og-image.mjs"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2"
  }
}
